===========================================================================================
Erros

    FastAPI 422 Unprocessable Entity no POST error.. mesmo mandando o JSON certo pelo fetch

    erro de CORS no FastAPI quando o front tá em porta diferente no localhost

    useEffect entrando em loop infinito quando atualizo o estado da lista

    como debugar state do react que não atualiza na hora mas aparece no console log depois

    pydantic error: field required .. mas o campo tá indo no body da requisição

    sqlite error: database is locked no python quando faço muitos inserts seguidos

    browser console alert: a parser-blocking, cross site script is invoked via document.write

    por que o z-index do modal não funciona se o pai tem transform ou filter?

===========================================================================================
Escolha de Bibliotecas

    vale a pena usar Axios em 2026 ou o Fetch nativo já resolve tudo pra CRUD simples?

    react-hook-form ou fazer validação na mão com useState: qual é mais produtivo?

===========================================================================================
Performance

    estilizar input type date pra não aparecer o ícone de calendário padrão

    otimizar re-render do react em tabelas com muitos itens usando useMemo

    css grid vs flexbox: qual o melhor pra fazer um dashboard com sidebar?

    como forçar o footer a ficar no final da página se o conteúdo for curto (sticky footer)

    padrão de projeto pra organizar pastas de uma api com FastAPI e pastas de schemas
